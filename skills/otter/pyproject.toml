[project]
name = "otter-skill"
version = "0.1.0"
description = "Otter.ai transcription CLI for Clawdbot"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "requests-toolbelt>=1.0.0",
]

[project.scripts]
otter = "scripts.otter:main"
