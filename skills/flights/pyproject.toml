[project]
name = "flights"
version = "1.0.0"
description = "Flight tracking and status alerts"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
]

[tool.uv]
dev-dependencies = []
